package core;

import inputHandler.*;

/**
 * Sandbox for testing library functions
 * @author South Salem High School
 *
 */
@SuppressWarnings("unused")
public class Driver {

	/**
	 * Put in any tests here.
	 * @param args This program takes no arguments.
	 */
	public static void main(String[] args) {
		
		/*
		InputType[] t = new InputType[3];
		String[] p = new String[3];
		String[] e = new String[3];
		t[0] = new InputType(InputType.INT);
		t[1] = new InputType(InputType.STRING);
		t[2] = new InputType("abc");//Ugh
		p[0] = "How many potatoes do you want?\nEx)\"4\"";
		p[1] = "What brand of potato do you want?\nEx)\"brown\"";
		p[2] = "Give me a word which contains \"abc\"";
		e[0] = "banana";
		e[1] = "HOW?";
		e[2] = "The word must contain \"abc\"";
		String[] userInputs = Templates.getInput(p,t,e);
		for(int i=0;i<userInputs.length;i++)
		{
			System.out.println(userInputs[i]);
		}*/
	}

}
